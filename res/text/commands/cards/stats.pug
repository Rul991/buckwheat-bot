include utils/mixins.pug

b 🃏 Статистика игрока 
    +link(user, false)
    | :
|
|
+pins([
    `Уникальных карточек: ${unique} / ${length}`,
    `Карточек в инвентаре: ${total}`
])