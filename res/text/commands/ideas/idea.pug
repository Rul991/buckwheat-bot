- let title = `📌 Идея от ${name}`
code [#{currentPage + 1} \ #{pagesLength}] 
b 
    if canVote
        | #{title}
    else 
        s #{title}
|
|
| #{text}
|
| 👍 - #{coolVote}                                      👎 - #{badVote}
|
|
b Создано: 
u #{date}
|  назад