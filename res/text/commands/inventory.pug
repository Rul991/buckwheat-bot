- let hasItems = items.length && !items.every(v => v.count == 'x0')

b Твой инвентарь:
|
|
|
if hasItems
    each item in items
        if item.count != 'x0'
            code 📍 #{item.name}
            |  - 
            b #{item.count}
            |
            |
    |
else 
    | (пусто)