mixin status(condition, text)
    if condition
        |
        |
        b #{text}
        |

mixin withTitle(title, text)
    |
    | #{title}: 
    b #{text}
    |

| 💫 
b #{description}
|
|
| 👾 Это - игрок 
a(href=link) #{name}
| . 

+withTitle(`${emoji} Ранг`, `${userNameRank} / ${status} (${rank})`)

if isLinked || isLeft || isMonkey
    |
    |

+status(isLinked, '💬 Привязан к данному чату')
+status(isLeft, '😵 Вышел из чата')
+status(rank <= 0 && isMonkey, '🐒 Не справился с кнопкой')
|
|

+withTitle(`${classEmoji} Класс`, className)
+withTitle('📈 Уровень', `${level}/${maxLevel} (${Math.floor(experiencePrecents * 100)}%)`)
+withTitle('✉️ Кол-во сообщений', `${messages}`)
+withTitle('⏰ Появление в чате', `${spawnDate} назад`)