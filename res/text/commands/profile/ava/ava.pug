include utils/functions.pug
include utils/mixins.pug

code [#{formatNumber(page + 1)} / #{formatNumber(length)}]
|  #{isCurrentImage ? '✔️' : '✖️'} Аватарка игрока 
+link(user)
|
+param('Создано', time)
b  назад