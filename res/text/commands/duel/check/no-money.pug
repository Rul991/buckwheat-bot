include utils/functions.pug
include utils/mixins.pug

| У игрока
+link(low)
| нет нужной суммы для дуэли!
| Тебе необходимо еще #{formatNumber(price - balance)} 💰!