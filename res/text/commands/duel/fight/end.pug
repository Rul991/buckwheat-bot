include utils/functions.pug
include utils/mixins.pug

mixin level(user)
        | Игрок 
        +link(user)
        | получает #{formatNumber(experience)} опыта!
        |

| Дуэль между 
+link(winner)
| и 
+link(loser)
| закончилась, победил 
+link(winner, false)
| !
| Он получает #{formatNumber(prize)}💰!
|
|
if experience > 0
    +level(winner)
    +level(loser)