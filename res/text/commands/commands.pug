- let replySymbol = '💬'

b 📌 По правилам я могу отвечать только на эти команды:
|
|
|
each command in commands
    | 👉 
    code баквит #{command.name}#{command.needData ? ` [${command.argumentText || 'аргументы'}]` : ''}
    |  #{command.replySupport ? replySymbol : ''}
    |
    each description in command.description.split('\n')
        |     ➖ #{description}
        |
    |
    |
blockquote 📍 Примеры времени, используемого в командах: 
    b 7д, 24ч, 60м, 30с, навсегда
|
blockquote 📍 Символ #{replySymbol} означает, что команда работает с ответами