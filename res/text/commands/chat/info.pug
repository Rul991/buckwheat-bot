mixin text(title, value)
    | 📍 #{title}: 
    b #{value}
    |
    |

mixin mark(title, bool)
    +text(title, bool ? '✅' : '🚫')

b 📌 Информация о чате:
|
|
|
+mark('Премиум', isPremium)
if isPremium
    +text('Время до конца премиума', untilDate)
+mark('Есть приветствие', hasHello)
+text('Количество правил', rulesLength)