include utils/mixins.pug
include utils/inventory.pug

mixin seller(user)
    |
    b 👤 Продавец: 
    +link(user)
    |

- const totalPrice = slot.price * needCount
- const priceText = `${formatNumber(slot.price)} * ${formatNumber(needCount)} = ${formatNumber(totalPrice)}💰`

+item({item, chance})
|
+param('📍 Цена', priceText)
+param('📍 Кол-во', formatNumber(slot.count))
|
|
|
+seller(seller)
+param('💲 Баланс', `${formatNumber(balance)}💰`)