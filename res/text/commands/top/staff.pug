b 📌 Иерархия чата:
each rating in ratings
    |
    |
    | #{rating.emoji} #{rating.rankName}:
    each player in rating.players
        |
        | #{player.isLeft ? '✖️' : '📍'}
        a(href=`tg://user?id=${player.id || id}`) 
            if player.isLeft
                s #{player.name}
            else
                | #{player.name}
|