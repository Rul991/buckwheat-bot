b 📌 Классы игроков:
|
|
|
- for(const [type, value] of Object.entries(classTitles)) {
    if classMembers[type].length && type != 'unknown'
        i #{emojies[type]} #{classTitles[type]}:
        |
        |
        each name in classMembers[type]
            | 📍 
            a(href=`tg://user?id=${id}`) #{name}
            |
            |
        |
        |
- }