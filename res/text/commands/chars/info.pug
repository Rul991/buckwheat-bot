include utils/mixins.pug

b 📌 Характеристики игрока 
+link({link, name}, false)
| :
|
+param('Класс', `${classEmoji} ${className}`)
+param('Здоровье', hp)
+param('Мана', mana, 4)