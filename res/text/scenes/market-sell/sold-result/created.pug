include utils/functions.pug
include utils/mixins.pug

| Игрок 
+link(user, false)
| , товар 
u #{item.name}
|  x#{formatNumber(count)} был выставлен на рынок за #{formatNumber(price)}💰 / ед!